
export function showRawMenu(options) {
  /*hnodemodMsg.send({
    dest: options.entity ? MsgDest.one : MsgDest.all,
    type: 'ShowMenu',
    entity,
    data: [
      { type: 'short', value: options.slots },
      { type: 'char', value: options.time || -1 },
      {}
    ]
  });
  WRITE_SHORT(slots);
  WRITE_CHAR(time);
  WRITE_BYTE(c ? TRUE : FALSE);
  WRITE_STRING(menu);
  MESSAGE_END();*/
}